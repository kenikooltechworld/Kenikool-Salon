{
  "name": "paystack-webhook-router",
  "version": "1.0.0",
  "private": true,
  "description": "Universal Paystack webhook router — one URL for all projects",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260726.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.114.0"
  }
}
